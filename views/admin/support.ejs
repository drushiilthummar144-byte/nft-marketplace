<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NFT Admin | Support</title>
    <link rel="stylesheet" href="/styles/main.css">
    <link rel="stylesheet" href="/styles/admin.css">
    <link rel="stylesheet" href="/styles/glass.css">
    <script src="https://unpkg.com/@phosphor-icons/web"></script>
    <style>
        .ticket-item {
            background: rgba(255, 255, 255, 0.02);
            border: 1px solid rgba(255, 255, 255, 0.1);
            padding: 20px;
            border-radius: 8px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            cursor: pointer;
            transition: 0.2s;
        }

        .ticket-item:hover {
            background: rgba(255, 255, 255, 0.05);
        }

        .priority-high {
            color: #FF5252;
            background: rgba(255, 82, 82, 0.1);
            padding: 4px 8px;
            border-radius: 4px;
            font-size: 0.75rem;
            font-weight: 600;
        }
    </style>
</head>

<body>

    <%- include('partials/sidebar') %>


        <main class="main-content">
            <header style="margin-bottom: 32px;">
                <h2>Support Tickets</h2>
            </header>
            <div class="ticket-list">
                <% if(tickets.length===0) { %>
                    <div style="text-align: center; padding: 40px; color: var(--text-muted);">No new support tickets.
                    </div>
                    <% } %>

                        <% tickets.forEach(ticket=> { %>
                            <div class="ticket-item">
                                <div class="flex gap-4 items-center">
                                    <div
                                        style="width: 40px; height: 40px; background: #2A1A40; border-radius: 50%; display: flex; align-items: center; justify-content: center; color: var(--text-muted);">
                                        #<%= ticket.id.toString().slice(-3) %>
                                    </div>
                                    <div>
                                        <div style="font-weight: 600; margin-bottom: 2px;">
                                            <%= ticket.message.substring(0, 40) %>...
                                        </div>
                                        <div style="font-size: 0.8rem; color: var(--text-muted);">
                                            <%= ticket.name %> (<%= ticket.email %>) • <%= ticket.date %>
                                        </div>
                                    </div>
                                </div>
                                <div class="flex items-center gap-6">
                                    <span class="priority-high"
                                        style="background: rgba(48, 133, 255, 0.1); color: #3085FF;">
                                        <%= ticket.status %>
                                    </span>
                                    <a href="mailto:<%= ticket.email %>" class="btn btn-primary"
                                        style="padding: 8px 16px; font-size: 0.85rem; text-decoration: none;">Reply</a>
                                </div>
                            </div>
                            <% }); %>
            </div>
        </main>
</body>

</html>